{
  "version": 2,
  "buildCommand": "cd shopsy-frontend && npm install && npm run build",
  "outputDirectory": "shopsy-frontend/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
